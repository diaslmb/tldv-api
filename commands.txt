apt-get update && apt-get install -y xvfb pulseaudio ffmpeg && pip install -r requirements.txt && playwright install && playwright install-deps && xvfb-run -n 88 -- sh -c 'pulseaudio --start --exit-idle-time=-1 && uvicorn main:app --host 0.0.0.0 --port 8080'
